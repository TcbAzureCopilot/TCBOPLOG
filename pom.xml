<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.machineroom</groupId>
  <artifactId>machineroom-log</artifactId>
  <version>1.0.0</version>
  <packaging>war</packaging>
  <name>MachineRoom Operation Log</name>
  <description>機房操作日誌系統 — Java EE 7 (Servlet 3.1) WAR for WebSphere Application Server + DB2</description>

  <properties>
    <!-- Java 8 so the WAR runs on traditional WAS 8.5.5 / 9.0 as well as Liberty -->
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <gson.version>2.10.1</gson.version>
    <openpdf.version>1.3.43</openpdf.version>
    <junit.version>4.13.2</junit.version>
    <h2.version>2.1.214</h2.version>
    <jetty.version>9.4.54.v20240208</jetty.version>
  </properties>

  <dependencies>
    <!-- Provided by WebSphere -->
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>3.1.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet.jsp</groupId>
      <artifactId>javax.servlet.jsp-api</artifactId>
      <version>2.3.3</version>
      <scope>provided</scope>
    </dependency>

    <!-- Bundled in WAR (no conflict with WAS-shipped libraries) -->
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>${gson.version}</version>
    </dependency>
    <dependency>
      <groupId>com.github.librepdf</groupId>
      <artifactId>openpdf</artifactId>
      <version>${openpdf.version}</version>
    </dependency>

    <!-- Test / local dev only -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>${h2.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>machineroom-log</finalName>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <!-- ship the DB scripts inside the WAR so db.autoInit / seeding can run them -->
      <resource>
        <directory>db</directory>
        <targetPath>db</targetPath>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <encoding>UTF-8</encoding>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.4.0</version>
        <configuration>
          <!-- also publish the compiled classes as a jar so the tcboplog (Spring Boot / Docker) module can reuse them -->
          <attachClasses>true</attachClasses>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <systemPropertyVariables>
            <machineroom.config>${project.basedir}/config/app.dev.properties</machineroom.config>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <!--
        Local development only:  mvn jetty:run  → http://localhost:8080/mrlog/
        Uses an embedded H2 database (DB2 compatibility mode) bound to the same
        JNDI name the WAR expects on WebSphere, and config/app.dev.properties
        (auth.mode=dev, db.autoInit=true).
      -->
      <plugin>
        <groupId>org.eclipse.jetty</groupId>
        <artifactId>jetty-maven-plugin</artifactId>
        <version>${jetty.version}</version>
        <configuration>
          <webApp>
            <contextPath>/mrlog</contextPath>
            <jettyEnvXml>${project.basedir}/src/test/jetty/jetty-env.xml</jettyEnvXml>
          </webApp>
          <systemProperties>
            <systemProperty>
              <name>machineroom.config</name>
              <value>${project.basedir}/config/app.dev.properties</value>
            </systemProperty>
          </systemProperties>
          <httpConnector>
            <port>8080</port>
          </httpConnector>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2.version}</version>
          </dependency>
        </dependencies>
      </plugin>
    </plugins>
  </build>
</project>
